
//--------------------------------------------------------------------------- Add hover to nav buttons


//--------------------------------------------------------------------------- Add Thread
function addThreadButton(event) {
	var clickedElem = event.target;
	if (clickedElem.classList.contains('new-thread-button')) {
		// show modal for making a thread
		// get info from thread to create database object



	}
}